"""Agent layer. Language only - never numbers.

Every module here takes engine output and returns prose. None of them may
compute, round, convert or count. See agents/explanation.py for how that is
enforced rather than merely intended.
"""
